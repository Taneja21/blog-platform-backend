server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/blog_auth
    username: postgres
    password: tushar123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
